routes:
  /featured/:
    controller: channel
    template: featured
    filter: featured:true

collections:
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
